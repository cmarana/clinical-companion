import type { BularioMedication } from "@/types/bulario";

/**
 * Bulário medication database.
 * Structure is ready — medications will be added in future phases.
 * Designed to scale to 10,000+ entries with virtualized rendering.
 */
export const bularioMedications: BularioMedication[] = [];
